import { StepObject } from '../guide';

function useFixed(stepInfo: StepObject, mask: boolean) {
  // const { id, dialogLocation, hightLightExtend, customDialog } = stepInfo;

  // let path:string = ''

  // if(mask) {
  //   strokeMask()
  // }


  // // 根据当前ID绘制遮罩层 如果存在的话
  // // 绘制遮罩
  // function strokeMask() {
  //   const width = document.body.clientWidth;
  //   const height = document.body.clientHeight
  //   path = ''
  // }
  // 定位当前弹窗
}

export default useFixed;
